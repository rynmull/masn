# Sprint Plan (2026-03-29 to 2026-04-11)

## Sprint Goal
Build the proprietary-voice foundation while improving reliability and test coverage so future deployment choices do not require architecture rework.

## Track A: Proprietary Voice Foundation

### A1. Provider-based TTS routing
- [x] Add provider fallback ordering (`proprietary -> native`, `elevenlabs -> native`, `native only`).
- [x] Add normalized cache-key builder inputs (text, emotion, engine, voice id, model version, pitch/rate).
- [x] Keep current user-facing behavior unchanged.

### A2. Engine readiness
- [x] Add `proprietary` as a first-class engine value in settings/runtime types.
- [x] Ensure DB setting reload accepts `proprietary`.
- [x] Preserve native fallback when proprietary/remote provider is unavailable.

### A3. Voice cache design prep
- [ ] Finalize key shape and invalidation notes.
- [ ] Define protected phrase list strategy (critical AAC phrases).

## Track B: Reliability & Testing

### B1. Unit tests for TTS engine logic
- [x] Test text normalization.
- [x] Test deterministic cache key generation.
- [x] Test provider fallback order by selected engine.

### B2. Unit tests for symbol utilities
- [x] Test provider-prefixed symbol generation.
- [x] Test fallback parsing behavior.
- [x] Test provider implementation flags.

### B3. Regression safety checks
- [x] Run `npm test -- --watch=false`.
- [x] Run `npm run lint`.
- [x] Run `npx tsc --noEmit`.

## Definition of Done
- New TTS abstraction helpers are in place and covered by tests.
- Current speech functionality still works with native and ElevenLabs.
- Project passes lint, tests, and type-check.

## Risks
- Expo managed workflow limits deep on-device neural inference.
- Proprietary engine runtime may require native/prebuild migration later.

## Deferred (Next Sprint)
- Implement proprietary speech provider runtime.
- Add phrase audio persistence layer (disk + SQLite index).
- Add benchmark harness for TTFA/RTF/MOS collection.